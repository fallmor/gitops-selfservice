apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: gitops-selfservice
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/fallmor/gitops-selfservice.git
  ref:
    branch: main
  secretRef:
    name: flux-system
